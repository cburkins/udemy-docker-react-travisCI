sudo: required
services:
    - docker

# container tag is only used inside Travis
before_install:
    - docker build -t cburkins/docker-react -f Dockerfile.dev .

script:
    - docker run cburkins/docker-react npm run test -- --coverage
